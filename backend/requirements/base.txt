# Shared runtime deps used by both the API and the worker today.
# As compute moves to the worker (Phase 1 restructure), pandas/numpy/yfinance
# will migrate from here into worker.txt so the API image stays lean.
pydantic==2.12.5
pydantic-settings==2.14.2
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
boto3==1.43.52
pandas==2.3.3
numpy==2.3.4
yfinance==0.2.66
requests==2.32.5
python-dateutil==2.9.0.post0
