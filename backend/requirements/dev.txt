-r api.txt
-r worker.txt

# Lint + format tooling (configured in backend/pyproject.toml)
ruff==0.9.6
black==25.1.0
isort==5.13.2

# Test tooling
pytest==8.3.4
httpx==0.28.1  # required by fastapi's TestClient
